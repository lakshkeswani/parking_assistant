class DirectionDetials {
  int distanceValue;
  int durationValue;
  String distanceText;
  String durationText;
  String encodedPionts;

  DirectionDetials();

  DirectionDetials.name(this.distanceValue, this.durationValue,
      this.distanceText, this.durationText, this.encodedPionts);
}
